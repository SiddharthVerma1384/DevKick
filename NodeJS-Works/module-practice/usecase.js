const y = require('./names')

const SayHi = (name) => {
    return (
        `Hi, ${name} you are passed as an onject.`
    )
}

// const {x} = y
console.log(y)
// console.log(SayHi(x))

require('./mind-game-export')